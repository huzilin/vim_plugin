#!/usr/bin/perl -w

@myNames = ('jacob', 'alexander', 'ethan', 'andrew');
@ucNames = map(ucfirst, @myNames);
print "$ucfirtst";
$numofucNames = map(ucfirst, @myNames);

foreach $key ( @ucNames ){
 print "$key\n";
}
print $numofucNames;
