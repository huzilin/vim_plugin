#!/usr/bin/python
#coding:utf-8

import struct

log = open('/opt/mysql-5.5/data/mybinlog.000005', 'rb')
log.seek(9431+19+6+2+1+1+1)

ret = struct.unpack('B',log.read(1))[0]
print ret
if ret:
    value = '%04d' % (1900 + ret)
else:
    value = "0000"
print value
